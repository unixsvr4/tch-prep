# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
*.tfplan.binary
.terraform.lock.hcl
tfplan.binary

# Ansible SSH demo key (generated by `make setup` — never commit private keys)
ansible/demo_key
ansible/demo_key.pub
docker/ansible-target/authorized_keys

# Ansible vault password file (contains the vault encryption password)
ansible/.vault_pass

# LocalStack data
.localstack/

# Environment overrides
.env

# macOS
.DS_Store
.README.md.swp
